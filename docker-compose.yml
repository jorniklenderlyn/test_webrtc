services:
  webrtc-app:
    build: .
    network_mode: "host"
    ports:
      - "443:8443"   # HTTPS port (not 8080)
    volumes:
      - ./certs:/app/certs:ro          # Mount certs
      - ./index.html:/app/index.html
      - ./client.js:/app/client.js
      - ./demo-instruct.wav:/app/demo-instruct.wav
    restart: unless-stopped